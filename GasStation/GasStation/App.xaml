 <Application x:Class="GasStation.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GasStation"
             xmlns:viewModels="clr-namespace:GasStation.ViewModels"
             StartupUri="MainWindow.xaml">    
    <Application.Resources>
        <!--<Style x:Key="MainStyleCheckBox" TargetType="{x:Type CheckBox}">
            <Setter Property="SnapsToDevicePixels"   Value="true" />
            <Setter Property="SnapsToDevicePixels"   Value="true" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <BulletDecorator Background="{TemplateBinding Background}">
                            <BulletDecorator.Bullet>
                                <StackPanel Orientation="Horizontal">
                                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Name="Border" Background="#FF595959" Cursor="Hand" >
                                        <Border Name="ChechedBorder" Width="26" BorderThickness="1" CornerRadius="4" Height="24" BorderBrush="DimGray" Background="#FF5A5A5A"></Border>
                                        <Label Content="{TemplateBinding Content}" Margin="2" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
                                    </Grid>
                                </StackPanel>
                            </BulletDecorator.Bullet>
                        </BulletDecorator>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True" >
                                <Trigger.Setters>
                                    <Setter TargetName="ChechedBorder" Property="Background"  Value="#FF61A5EF"/>
                                    <Setter TargetName="Border" Property="Background"  Value="#FF9A9A9A"/>
                                </Trigger.Setters>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Trigger.Setters>
                                    <Setter TargetName="ChechedBorder" Property="Background"  Value="#FF5A5A5A"/>
                                    <Setter TargetName="Border" Property="Background"  Value="#FF595959"/>
                                </Trigger.Setters>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->


    </Application.Resources>
</Application>
