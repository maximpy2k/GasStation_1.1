<Window x:Class="GasStation.ModalWindows.View.WindowRrgParams"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GasStation.ModalWindows.View"
        xmlns:conv="clr-namespace:GasStation.ViewModels.Converters"
        xmlns:elem="clr-namespace:GasStation.UserControls.UserControlsView"
        mc:Ignorable="d"
        Title="Настройки РРГ" Height="200" Width="300">
    <Window.Resources>    
        <conv:ClassConverterRegime x:Key="ConvRegime"/>
    </Window.Resources>
    <StackPanel >
        <StackPanel Orientation="Horizontal">
            <Label FontSize="22" Content="РРГ  №" Margin="5"></Label>
            <Label FontSize="22" DataContext="{Binding}" Content="{Binding Path=Num}" Margin="5"></Label>
        </StackPanel>
        <ComboBox Height="20" Margin="5"                   
                  ItemsSource="{Binding Source={StaticResource ConvRegime}, Path=Items}"
                  IsEnabled="{Binding EnabledChangedScript}"
                  Text="{Binding Path = TypeReg, Mode=TwoWay, Converter={StaticResource ConvRegime}}"/>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Height="20"  Width="110" Margin="2">
                <TextBlock.Resources>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=UsePid, Path=IsChecked}" Value="True">
                                <Setter Property="Text" Value="Заданное давление"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ElementName=UsePid, Path=IsChecked}" Value="False">
                                <Setter Property="Text" Value="Заданный расход"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Resources>
            </TextBlock>
            <TextBox IsEnabled="{Binding EnabledChangedScript}" Height="20" Width="70" Margin="2" Text="{Binding Path=SetupValue}"/>
        </StackPanel>
        <CheckBox Content="Закрытие клапана"
                  IsEnabled="{Binding Flap.EnabledChangedScript}"
                  IsChecked="{Binding  Path=Flap.FlapState}"
                  Visibility="{Binding  Path=RrgConst.IsFlapPropertyVisible}"/>
        <CheckBox x:Name="UsePid" Content="Использование ПИД регулятора"                 
                  Visibility="{Binding Path=RrgConst.EnablePid}"
                  IsChecked="{Binding UsePid}"/>
        <!--IsEnabled="{Binding EnabledChangedScript}"
                  IsChecked="{Binding  Path=FlapState}"-->
        <CheckBox Content="Разрешение пользователю изменять свойства" IsChecked="{Binding UsePriv}" IsEnabled="{Binding Path=EnabledChangedScript}"/>
        
    </StackPanel>
</Window>
