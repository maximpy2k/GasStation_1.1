<Window x:Class="GasStation.ModalWindows.View.WindowsParams.WindowShutterParams"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GasStation.ModalWindows.View.WindowsParams"
        mc:Ignorable="d"
        Title="Настройки затвора" Height="153.418" Width="300">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label FontSize="22" Content="Затвор №" Margin="5"></Label>
            <Label FontSize="22" DataContext="{Binding}" Content="{Binding Path=ShutterConst.DevNum}" Margin="5"></Label>
        </StackPanel>
        <CheckBox Content="Открытие клапана 1"
                  IsEnabled="{Binding EnabledChangedScript}"
                  IsChecked="{Binding  Path=Flaps[0].FlapState}"/>
        <CheckBox Content="Открытие клапана 2"
                  IsEnabled="{Binding EnabledChangedScript}"
                  IsChecked="{Binding  Path=Flaps[1].FlapState}"/>
        <CheckBox Content="Разрешение пользователю изменять свойства"  IsChecked="{Binding Path=UsePriv}" IsEnabled="{Binding Path=EnabledChangedScript}"/>
    </StackPanel>
</Window>
