<UserControl x:Class="SoursePrj.UserControls.UserControl_VacuumGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Width="100" Height="68.334">
    <UserControl.Resources>
        <Style x:Key="CheckBoxStyle1" TargetType="{x:Type CheckBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid>
                            <Rectangle x:Name="rectangle" RadiusY="5" RadiusX="5" Stroke="Black">
                                <Rectangle.Fill>
                                    <RadialGradientBrush RadiusY="0.9" RadiusX="0.9">
                                        <GradientStop Color="Black" Offset="1"/>
                                        <GradientStop Color="#FFA0E65B"/>
                                    </RadialGradientBrush>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Fill" TargetName="rectangle">
                                    <Setter.Value>
                                        <RadialGradientBrush RadiusY="0.9" RadiusX="0.9">
                                            <GradientStop Color="Black" Offset="1"/>
                                            <GradientStop Color="#FFE51400"/>
                                        </RadialGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True"/>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Left" Width="100" Height="68.334" VerticalAlignment="Top">
        <Rectangle Stroke="Black" VerticalAlignment="Top" Fill="#FFD8D295" RadiusX="5" RadiusY="5" HorizontalAlignment="Left" Width="100" Height="68.334" Margin="0"/>
        <Label x:Name="label" Content="Вакууметр" Margin="-1.772,-5.251,17.927,0" FontSize="14" FontWeight="Bold" Height="28.953" VerticalAlignment="Top"/>
        <GroupBox  Header="Давление" Margin="5.75,15.578,16.588,2.423" BorderBrush="Black" FontWeight="Bold" d:LayoutOverrides="HorizontalAlignment">
            <GroupBox.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF355119" Offset="1"/>
                    <GradientStop Color="#FFB2FF66"/>
                </LinearGradientBrush>
            </GroupBox.Background>
            <TextBox Height="27.032" IsReadOnly="True" Margin="-1.654,0.17,-1.654,0.171" TextWrapping="Wrap" VerticalAlignment="Center" FontWeight="Bold" Background="{x:Null}" BorderBrush="Black" FontSize="18" HorizontalAlignment="Center" Width="68.641"
                Text="{Binding Path=CurrentPress}"/>
        </GroupBox>
        <CheckBox  HorizontalAlignment="Right" Visibility="{Binding Path=EnableOut, Mode=OneWay}" IsChecked="{Binding PressDio}" IsEnabled="False" Height="19.004" Margin="0,2.433,2.698,0" Style="{DynamicResource CheckBoxStyle1}" VerticalAlignment="Top" Width="19.612"/>
    </Grid>
</UserControl>
