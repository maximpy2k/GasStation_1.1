<UserControl x:Class="SoursePrj.Брянск.Камера.UserControl_Chamber"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SoursePrj"
             xmlns:local2="clr-namespace:SoursePrj.UserControls"
             mc:Ignorable="d" Width="450" Height="400">
    <UserControl.Resources>
        <Style x:Key="CheckBox_Error" TargetType="{x:Type CheckBox}">
            <Setter Property="IsEnabled" Value="False"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid>
                            <Ellipse x:Name="ellipse" Stroke="Black" StrokeThickness="0.1">
                                <Ellipse.Fill>
                                    <RadialGradientBrush RadiusY="0.527" RadiusX="0.567">
                                        <GradientStop Color="Green" Offset="1"/>
                                        <GradientStop Color="White"/>
                                    </RadialGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="ellipse" Property="Fill">
                                    <Setter.Value>
                                        <RadialGradientBrush RadiusY="0.527" RadiusX="0.567">
                                            <GradientStop Color="Red" Offset="1"/>
                                            <GradientStop Color="White"/>
                                        </RadialGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <local:UserControlChamber HorizontalAlignment="Left"
            DataContext="{Binding Chambers[0]}" />
        <CheckBox IsChecked="{Binding Chambers[0].ChamberView.IsWater}" IsEnabled="False" Content="CheckBox" Style="{DynamicResource CheckBox_Error}" HorizontalAlignment="Right" Width="33" Height="33" VerticalAlignment="Bottom" Margin="0,0,213,152" RenderTransformOrigin="-0.061,0.606" />
    </Grid>
</UserControl>
